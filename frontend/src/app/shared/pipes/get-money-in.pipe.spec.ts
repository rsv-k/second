import { GetMoneyInPipe } from './get-money-in.pipe';

describe('GetMoneyInPipe', () => {
  it('create an instance', () => {
    const pipe = new GetMoneyInPipe();
    expect(pipe).toBeTruthy();
  });
});
